# HANDOFF — Git에서 claim-v3 서버리스 사용 (2026-09-09)

현재 프로젝트의 기본 UI/CLI는 RunPod `fdiltabt78bogm`의 **claim-v3**를 요청합니다.
Windows 설치는 `setup-test.cmd`, 설정 확인은 `.venv/Scripts/python.exe serving/launch_test.py --check`,
실제 사용 시 화면 실행은 `start-test.cmd`입니다. [실행 설명](serving/README.md)을 참고하세요.
이번 Git 정리에서는 모델 추론이나 GPU 가동을 요청하지 않았습니다.
비공개 자료와 `.env`가 없는 staged 체크아웃에서 오프라인 테스트 13개를 통과했고,
외부 연결을 차단한 UI 로딩 검사에서도 외부 접속 시도가 없었습니다.
설정 JSON에 API 키가 노출되지 않고 자동 추론 이벤트가 없는 것을 확인했습니다.
커밋 대상 25개 파일에서 로컬 인증정보 값과 주요 토큰 패턴을 검사했습니다.

## 모델 및 복구 정보

- 새 비공개 모델: `Mepeng22/gemma-4-31b-claim-rl-v3`
- 실제 HF 재다운로드/GPU 로딩 검증 revision: `e5aac01e9fe0af54b17e7f52b66486112183035e`
- adapter SHA256: `ab3be6f0019b3af83798da66151ac3614626fceb75fbff7c8376fe26646fbba0`
- 기반 모델: `google/gemma-4-31B-it` @ `842da3794eaa0b77d5f08bae87a17459d91ff475`
- 보존된 v2: `Mepeng22/gemma-4-31b-claim-lora-v2` @ `cc04579366bbb88663029d53b2aafcc84159589e`
- v3 템플릿: `9gl9uhysjl`; 복구용 v2 템플릿: `99icte8riw`.

v3는 v2에서 이어서 16 optimizer updates를 수행한 누적 어댑터입니다.
원본 v2와 학습 재개 상태는 비공개 보관되어 있습니다. v2 복구는 RunPod 템플릿 및
클라이언트 모델 별칭을 함께 바꾸어야 하며, 이 문서/설치 스크립트는 복구를 실행하지 않습니다.

## 확인된 범위와 미해결 사항

이전 실행에서 새 모델의 HF 재다운로드와 전체 BF16+PEFT 로딩, v3 서버리스 자문 응답을
확인했습니다. 이후 재배포에서는 설정 변경만 확인했고 워커 미배정으로 테스트 요청을
취소했습니다. **최근 재배포의 새 추론 응답은 미검증**입니다.

파일럿 최종 20개 평가의 자동 평균은 v2 .825 → v3 .8325였습니다. 청구항 계약은
1/8 → 0/8, 어노테이션은 0/4 → 0/4, 자문 계약/인용은 12/12 → 12/12였습니다.
자동 점수는 법률적 정확도 백분율이 아니며, 심각한 도면/판례 사실 혼입을 놓친 사례가
있었습니다. 전반적인 품질 향상을 주장하지 않습니다. 이전 실제 어노테이션 응답에는
반복/잘림 문제가 있었고, 도면 요청은 시간 초과였습니다.

마지막 운영 점검에서 min=0/max=1/idle=5초, FlashBoot OFF였고 실제 GPU·학습 Pod·
유료 네트워크 볼륨 0, 자동 충전 OFF를 확인했습니다. 이는 과거 점검 기록이며
현재 상태를 자동으로 감시하는 프로그램은 없습니다. 요청하면 유료 워커가 생길 수 있습니다.

공개 Git에는 코드와 설정 예제만 포함합니다. 토큰, 비공개 데이터/모델, 원문 평가 출력,
계정 비용 및 실행 로그는 로컬과 비공개 보관본에 유지합니다. 상세 비용·종료 증거를 담은
이전 HANDOFF는 로컬 `run_artifacts/git_serverless_20260909/HANDOFF.private.before.md`에
보존했습니다. 비공개 자료가 없는 새 체크아웃에서도 기본 도면 UI와 CLI를 사용할 수 있습니다.
