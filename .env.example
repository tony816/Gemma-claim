# Copy to .env; never commit credentials. Nonempty project .env values win.
# Local UI and CLI use RunPod only. No HF token is needed for remote inference.
RUNPOD_API_KEY=
RUNPOD_ENDPOINT_ID=fdiltabt78bogm
# 1 blocks all requests from this client; this does not change RunPod workers.
CLAIM_ENDPOINT_PAUSED=0

# Optional: only for the separate historical Hub publishing pipeline.
# HF_TOKEN=
# HF_USER=
# HF_ADAPTER_REPO=
# HF_MERGED_REPO=
# HF_PRIVATE=1
