# 서버리스 v3 변경 — 2026-09-09

사용자가 앞선 품질 실패 보고를 받은 뒤 새 강화학습 모델로 변경을 요청하여, 기존 endpoint `fdiltabt78bogm`의 모델 설정과 로컬 테스트 화면을 **claim-v3**로 변경했다. 이번 작업은 추가 학습이나 품질 재평가가 아니다.

- 비공개 모델: https://huggingface.co/Mepeng22/gemma-4-31b-claim-rl-v3
- 고정 모델 리비전: `e5aac01e9fe0af54b17e7f52b66486112183035e`
- 어댑터 SHA256: `ab3be6f0019b3af83798da66151ac3614626fceb75fbff7c8376fe26646fbba0`
- 새 비공개 템플릿: `9gl9uhysjl`. 기존 v2 템플릿 `99icte8riw`와 v2 원본은 보존했다.
- 기반 모델 BF16와 tokenizer 리비전: `842da3794eaa0b77d5f08bae87a17459d91ff475`.
- 설정: min=0, max=1, idle=5초, FlashBoot OFF, request-count scaling 1. 요청이 없으면 상시 GPU를 두지 않는다.
- 저장소·가중치·서빙 이미지·bootstrap 코드 해시는 이전 실제 GPU 재로딩과 실제 claim-v3 자문 응답을 확인한 구성과 같다. 기존 파일럿의 청구항/어노테이션 오류는 그대로 남아 있다.

## 이번 요청에서 확인된 범위

새로운 짧은 도면 요청을 실제 Gradio 콜백으로 제출했지만, RunPod가 워커를 배정하지 않아 이번 재배포의 새 추론 응답은 확보하지 못했다. 원인이 특정 GPU 재고 부족인지 다른 플랫폼 배정 문제인지 확정하지 않았다. **설정 변경 완료와 이번 실시간 추론 미검증을 구분한다.** 이전 모델 로딩·응답 증거를 새 테스트 성공이라고 다시 집계하지 않는다.

A100 요청 대기 중 H100 80/94GB 및 RTX PRO 6000 Blackwell Server 96GB도 허용했으나, 이번에는 어떤 GPU도 배정되지 않았다. 모든 경우 GPU 수 1개/최대 워커 1개와 기존 종료 시한·추가 $12 상한을 유지했다. GPU별 현재 표기 단가는 A100 $2.72/시간, RTX PRO 6000 $3.49/시간, H100 $4.79/시간이며 실제 사용 기동·처리·종료 대기와 컨테이너 디스크 비용이 붙는다. 출처: https://www.runpod.io/pricing 및 https://docs.runpod.io/serverless/pricing .

대기 중이던 이번 테스트 한 건은 purge-queue 응답 `removed: 1`로 취소했다. 이후 진행/대기 작업 0, 실제 GPU Pod 0, 학습 Pod 0, 네트워크 볼륨 0, 시간당 현재 과금 0, Auto-pay OFF를 확인했다. 초기 테스트용 계정 제어 키와 종료 시한을 새 템플릿에서 제거하고 로컬 종료 감시도 중지했다. 나중에 임시 타이머 때문에 v2로 돌아가는 설정은 남기지 않았다.

현재 잔액은 **$9.177295**이고, 이번 재배포 시도의 관측 잔액 차감은 **$0.00**이다. 이전 작업의 차감과 합치지 않았다. billing API 원본은 `closing_audit.json`에 보존했다. 이 값은 조회 당시 관측이며 확정 청구서로 부풀리지 않는다.

## 테스트 방법

`start-test.cmd`를 실행한 뒤 http://127.0.0.1:7860/ 에서 새로고침한다. 선택 모델은 v3이고 청구항 생성 및 확정 예제 시험 버튼이 활성화되어 있다. `serving/claim_client.py`와 `space/claim_client.py` 모두 claim-v3를 기본값으로 사용하며, `serving/deployment_status.json`은 paused=false이다.

도면을 올려 생성하거나 아래 확정 예제에서 판례 어노테이션·KR/US 자문을 선택할 수 있다. 워커 배정 및 초기 로딩에 수 분 이상 걸릴 수 있으며, 현재와 같이 배정이 지연되면 응답 대기 한도에서 취소된다. 반복·도면 근거·어노테이션 실패를 해결했다고 주장하지 않는다. 모델 품질 결과는 `RL_RESULTS.md`의 이전 평가를 따른다.

## 증거

`run_artifacts/rl_v3_redeploy_20260909/`에 scope, 원래 가중치 재로딩 영수증 사본, 새 배포 설정, CPU 24개 검사 결과, UI 활성 상태, 대기열 진단, 테스트 취소, 임시 제어 키 제거, 기존 모델 보존과 최종 계정 상태를 기록했다. 이전 증거는 `run_artifacts/rl_v3_finalize/`에서 그대로 확인할 수 있다.
