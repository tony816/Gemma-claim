# Gemma claim v3 추가 강화학습 결과 — 2026-09-09

> 최신 운영 상태: 사용자의 후속 요청으로 서버리스와 화면 설정을 v3로 변경했다. 이번 새 테스트는 GPU 워커 미배정으로 응답을 확보하지 못했고 취소했다. min=0/max=1, Auto-pay OFF. 자세한 상태는 [SERVERLESS_V3_UPDATE.md](SERVERLESS_V3_UPDATE.md)를 따른다. 아래 품질 평가와 이전 배포 실패 기록은 변경하지 않았다.

상태: **전체 요청 미완료**. v2에서 추가 RL, 평가, 비공개 저장과 실제 GPU 재로딩은 완료했다. 서버리스에서 새 LoRA로 실제 자문 응답을 확인했으나 도면 시간 초과와 어노테이션 반복·잘림으로 배포 검증이 실패했다. 기존 v2로 복구하고 요청·과금 자원을 중지했다.

## 모델과 복구

- 비공개 모델: https://huggingface.co/Mepeng22/gemma-4-31b-claim-rl-v3
- 실제 GPU 재로딩 검증 및 서버리스 고정 리비전: `e5aac01e9fe0af54b17e7f52b66486112183035e`.
- 누적 어댑터 SHA256: `ab3be6f0019b3af83798da66151ac3614626fceb75fbff7c8376fe26646fbba0`.
- 출발 v2: `Mepeng22/gemma-4-31b-claim-lora-v2` @ `cc04579366bbb88663029d53b2aafcc84159589e`. 원본은 수정하지 않았다.
- v2 가중치 SHA256: `0b4731babca803200bdab51e17aa08155370a20bd7b54f887bfef16faccfdd55`.
- 기반 모델: `google/gemma-4-31B-it` @ `842da3794eaa0b77d5f08bae87a17459d91ff475`, BF16.
- 과거 v2 학습 당시 기반 가중치의 정확한 SHA는 복원하지 못했다. 프로세서 캐시 SHA를 기반 가중치 SHA로 대체하지 않았으며, 이번 실행에서 호환성을 확인한 위 리비전을 고정했다.
- 산출물은 v2와 RL 변경을 모두 포함한 누적 PEFT LoRA이다. 별도의 v2 어댑터를 겹쳐 로드할 필요가 없다. 기반 모델 접근 권한은 필요하다.
- rank 16, alpha 32, 언어 모델 410개 모듈/820개 LoRA 텐서, 학습 가능 파라미터 122,429,440개.

`run_artifacts/rl_v3_finalize/pod/fresh_reload_verified.json`은 고정 commit의 7개 파일을 새 경로로 내려받아 해시를 검증하고, 전체 BF16 기반 모델과 어댑터를 실제 GPU에서 다시 로드해 저장 텐서 일치 및 도면 입력 추론을 확인한 기록이다. 파일 존재 확인이나 CPU 설정 검사만으로 대체하지 않았다. 재로딩 검증 후 학습 Pod `s8pcn4cgdmolee`를 삭제했다. 후속 문서 업로드 commit과 이 가중치 검증 commit은 구분한다.

추가 학습 재개용 상태는 같은 비공개 저장소의 commit `dff626e2ff255a690c33c6b98f311635a7bec8d8`, 경로 `runs/resume-20260909/checkpoints/step-0016/`에 있다. 어댑터·optimizer·CPU/CUDA RNG 등 8개 파일의 해시를 검증했다. 로컬 백업은 `run_artifacts/rl_v3_resume/pod/checkpoints/step-0016/`이다. 현재 `rl_training/config.json`은 학습이 아닌 복구 전용 설정이므로, 다음 학습 전에 새 실행 디렉터리·예산·기한·재개 지점을 명시해야 한다.

## 실제 학습 범위

확정본 `data/case_rl/releases/rl-pilot-27fc35d39288134b`의 283개 파일 해시를 검증했다. manifest SHA256은 `58836cafc6b27c091f3a5dafccd92d40d17b36ff64828392782d257c34c82706`이다. 한국·미국 판례 각 20건을 검토한 파일럿이며 전체 판례 DB 학습이 아니다. 학습 풀 88개, calibration 20개, 최종 test 20개를 유지했다. 정책에는 메시지·도면·제공 카드만 전달했다. 기존 시험 자료 75개는 해시 `db5533a8c06cd97fff8b953b15c2f5b9da36cff631947479d69182c0f366fe3c`로 보존했다.

RunPod H200 한 대에서 생성 결과의 보상으로 정책을 업데이트하는 on-policy group-relative REINFORCE를 실행했다. SFT/DPO만 수행한 작업이 아니다. 그룹 크기 2, 생성 온도 0.8, top_p 1, 최대 새 토큰 768, AdamW 학습률 5e-7, gradient clip 0.5, KL 계수 0.02이다. KL 참조는 RL 이전의 정확한 v2 정책이며, 참조 텐서 불변 여부를 검사했다. 의미 평가와 출력 계약·심각한 오류 제한을 함께 사용했다. 양의 동점 차이는 0.01이며 작은 보상 차이의 과도한 확대를 제한했다. 이미지·주제별 학습 순서는 seed 20260909로 고정했다.

최종 선택된 경로는 17개 에피소드에서 16번 optimizer 업데이트를 거친 step16이다. 중단된 실행의 9–12번 업데이트를 재실행했기 때문에 실제 유료 실행의 optimizer 작업 수와 최종 경로 step 수는 다르다. 앞선 거절된 1-step 파일럿까지 포함하면 전체 작업에서 21번 optimizer 연산이 실행되었으며, 최종 모델에 21 step이 누적된 것은 아니다. 최종 복구 Pod에서는 추가 optimizer 업데이트가 0회였다.

step8/16 비교 규칙은 사전 고정했다. 교정된 calibration 출력 계약은 v2 13/20, step8 12/20, step16 13/20이다. step16의 평균 보상·심각 오류·계약 통과가 기준을 충족하여 최종 test 전에 선택했다. 최종 test 점수가 낮은 항목을 보고 모델을 다시 선택하지 않았다.

## 평가 결과와 해석

동일한 정책 입력과 greedy 생성, 최대 새 토큰 768로 비교했다. 아래 점수는 자동 채점 rubric 평균이며 법률 정확도 백분율이 아니다.

| 지표 | v2 calibration | v3 calibration | v2 test | v3 test |
|---|---:|---:|---:|---:|
| 전체 자동 평균, 20개 | 0.7850 | 0.8050 | 0.8250 | 0.8325 |
| 청구항 자동 평균, 8개 | 0.4625 | 0.5125 | 0.5625 | 0.58125 |
| 자동 심각 오류, 20개 | 5 | 5 | 4 | 3 |
| 교정된 전체 출력 계약 | 13/20 | 13/20 | 13/20 | 12/20 |
| 청구항 출력 계약 | 1/8 | 1/8 | 1/8 | 0/8 |
| 제공 카드에 비어 있지 않은 어노테이션 | 0/4 | 0/4 | 0/4 | 0/4 |
| 자문 계약 및 비어 있지 않은 근거 인용 | 12/12 | 12/12 | 12/12 | 12/12 |

최종 test에서 v2/v3 모두 독립항 1개와 종속항이 존재하는 출력은 7/8이었다. 그러나 항 본문의 명시적 인용, 중복, 도면 근거 등을 함께 보면 작성 품질 목표를 달성했다고 할 수 없다. 청구항 중복 사례는 1→2개로 늘었다. 자문은 KR 6개·US 6개 모두 제공된 근거 ID가 연결되지만, 관할·기준 시점·반대 논거의 충실성까지 입증하는 수치는 아니다.

별도 채점 모델은 `Qwen/Qwen3-VL-32B-Instruct` @ `0cfaf48183f594c314753d30a4c4974bc75f3ccb`, NF4 double quantization/BF16이다. RunPod에서 실제 24개 blind calibration 선호 비교와 심각 오류 probe를 통과한 구성을 사용했다. 이전 자료 검토 24/24를 GPU 채점 검증으로 대신하지 않았다. 다만 후속 수동 검토에서 심각한 오탐 누락을 확인했으므로, 이 calibration 통과만으로 채점기의 신뢰성을 일반화하면 안 된다.

- `US20240001357A1` 도면 4장을 직접 검토했다. v2와 v3 원문이 완전히 같으며, 제공된 KR 판례의 약액 저장통·이동축 서술과 근거 없는 무잔류 완전 배출 성능을 도면 청구항에 가져왔다. 판례 자체가 그 기능의 구조적 뒷받침 부족을 지적하는데도 자동 채점기는 양쪽에 1.0과 심각 오류 없음으로 판단했다.
- `US20240066518A1`은 원문·계약 검토 범위이다. v2는 어댑터/슬리브를 재귀적으로 반복하다 잘렸고 v3도 반복과 종속항 인용 누락이 남았다. 이 사례의 도면 8장 전체를 독립적으로 검토했다고 주장하지 않는다.
- KR `2020허6323` 자문은 E1/E2의 TTL/F-number 논거를 양쪽 모두 사용했지만 관할·기준 날짜 명시와 충분한 반대 논거가 약했다. 뚜렷한 내용 개선을 확인하지 못했다.
- US Enzo 사례는 v3가 2010-03-26의 역사적 범위와 bulky-linker 반론을 더 명시했고, v2는 수치 기준 추가 시 신규 사항 위험을 더 명시했다. 자동 1.0은 이런 차이를 구분하지 못한다.
- 학습 중 `US20070059204A1`도 도면과 다른 기술을 생성했는데 의미 채점기가 높게 평가했다. 출력 계약 보상 제한이 실제 보상을 0/0.25로 낮췄지만, 의미 채점 자체의 결함은 남았다.

따라서 이번 결과는 저장·재개 가능한 소규모 RL 실험이다. 자동 평균의 소폭 상승을 전반적인 청구항·어노테이션·법률 자문 성능 향상으로 보고하지 않는다. 다음 학습에서는 도면과 판례 기술 구성의 혼입, 부정 근거에 대한 잘못된 적용, 종속항 연결, 어노테이션 적용 설명 및 채점기 누락을 먼저 보강해야 한다.

평가 도구의 기존 split-only 카드 조회가, held-out 도면 예제에 명시적으로 제공된 train 카드를 찾지 못하는 결함을 수정했다. 변경된 resolver는 명시적으로 제공되고 해시가 검증된 train+현재 split 카드만 허용한다. 기존 원문과 자동 점수를 보존한 채 재검증했으며, 모델 선택은 step16으로 유지됐다. 비교의 원래 pipeline 수치는 `comparison.json`에 별도로 남아 있다.

## 실패와 복구 기록

초기 두 judge 실행은 JSON/선호 일치 기준 문제로 중단됐다. 첫 1-step RL 모델은 calibration이 나빠져 거절했고 `rejected-pilot-step1-20260909` 태그로 보존했다. 후속 실행에서는 로컬 종료 감시 코드가 이미 시작된 Pod의 일시적인 접속 정보 누락을 시작 실패로 오인해 조기 삭제했다. 이는 운영 코드 결함이며 step9–12가 유실됐다. step8의 HF checkpoint와 새 로컬 해시 백업에서 재개했고, 감시 코드에 해당 상황에서 재시도/Stop을 사용하도록 수정 및 회귀 검증했다.

재개 실행은 step16 선택 및 v3 test 20개를 마쳤으나 v2 test의 Enzo 채점 결과 JSON 문자열에 literal TAB이 들어가 중단됐다. checkpoint16을 보존한 뒤 복구 전용 Secure H200에서 남은 v2 14개 평가와 최종 export/reload를 실행했다. JSON 구조를 고치거나 점수를 채워 넣지 않고, 문자열 안의 literal control 문자만 허용하는 전송 파서를 사용했다. 이전 유효 응답 79개는 해석 결과가 같고 구조가 깨진 음성 예제는 계속 거절했다. GPU에서 실패한 Enzo 입력 packet과 raw judge 문자열이 정확히 재현됐으며 1.0으로 정상 파싱됐다. 정책 출력 JSON 파서는 계속 strict이다.

중단 당시 실제 코드/확정 payload는 비공개 commit `217b134240563bec775ca96a53a3fd4afc6109fd`의 `runs/resume-20260909/original_payload.tar.gz`에 있다. 현재 소스의 parser 수정이 과거 학습 때 사용된 것처럼 보고하지 않는다. 중간 실패에서는 최종 루트 모델의 GPU 재로딩 전에 Pod가 종료된 사례가 있었으며, 최종 성공 실행의 저장→재로딩→삭제 순서와 구분한다.

## 테스트와 운영

로컬 `start-test.cmd`를 실행하고 http://127.0.0.1:7860/ 에 접속한다. **현재는 v3 배포 실패 안내와 호출 중지 상태이며, 생성 버튼 및 API 콜백이 유료 요청을 막는다.** 확정 예제와 제공 근거는 열람할 수 있다. 실제 v3 자문·어노테이션 실패 원문은 `run_artifacts/rl_v3_finalize/serving/ui_smoke.json`에서 확인한다. 향후 재배포 검증 후 `serving/deployment_status.json`의 paused 상태와 클라이언트 모델 설정을 함께 변경해야 한다. 단순 화면 실행은 워커를 깨우지 않는다.

UI의 요청 문구와 출력 한도는 고정 held-out 비교와 다르다. UI 성공 여부는 기능 연결 검증이며 동일 조건 RL 성능 향상의 증거로 사용하지 않는다. 원문은 변경하지 않고 표시하며, 형식 파싱 실패는 오류로 드러낸다. 판례 카드는 제공된 근거이지 가중치 내부의 인과적 출처를 증명하는 장치가 아니다.

기존 endpoint ID `fdiltabt78bogm`을 유지한다. 실패한 새 서빙 시도는 전체 BF16 기반 모델과 누적 LoRA를 A100 80GB 1개에서 사용하며, 이전 QAT INT4/A40 구성과 요청 비용·cold start가 다르다. 0.28.0 worker-vLLM 이미지와 base/tokenizer/adapter SHA를 고정하고 시작 시 7개 파일 해시를 검증한다. API 응답의 실제 모델 이름은 `claim-v3`이어야 한다.

기존 v2 서버리스 템플릿 `99icte8riw`와 원본 v2 모델을 보존했다. 복구 명령은 `.venv\Scripts\python.exe -m rl_training.serve rollback`이며, v2 템플릿을 min=0/max=0으로 복구한다. 이후 v2를 호출하려면 CPU 클라이언트 두 사본의 `TUNED_MODEL`을 `claim-v2`로 되돌리고, 명시적인 새 테스트 예산 하에 max=1로 열어야 한다. 단순 rollback 명령은 유료 워커를 시작하지 않는다.

최종 비용·실제 UI 응답·워커 0 확인·Auto-pay 상태는 아래 확정 운영 영수증을 따른다. 요청 기반 서버리스는 호출, 기동, 처리, 종료 전 대기 동안 과금될 수 있다. min=0과 자동 충전 해제는 요청 자체를 무료로 만들지 않는다.

## 주요 증거

- `run_artifacts/rl_v3_finalize/comparison.json`: 원문 기준 교정된 v2/v3 calibration/test 비교.
- `run_artifacts/rl_v3_finalize/manual_test_review.json`, `manual_train10_review.json`: 제한된 수동 검토와 실패 사례.
- `run_artifacts/rl_v3_finalize/pod/`: 실제 GPU 환경·로딩·선택·평가·재다운로드 영수증.
- `run_artifacts/rl_v3_finalize/serving/`: 배포 설정, 시작 시 해시 검증 로그, 실제 UI 요청 및 종료 관측.
- `run_artifacts/rl_v3_followup/incident/`: 조기 삭제 운영 결함.
- `run_artifacts/rl_v3_resume/checkpoint_local_backup.json`: optimizer/RNG 포함 step16 로컬 복구 검증.
- `run_artifacts/rl_v3_finalize/closing_audit.json`: 최종 계정·자원·잔액·기존 v2/데이터 보존 감사.


## 확정 운영 결과

- UI 도면: 760초 대기 한도 초과. 정상 원문 확보 실패.
- UI 어노테이션: 실제 `claim-v3` 응답이 캡·펌프·밸브를 반복하다 1,536토큰에서 종료되어 JSON/어노테이션 계약 실패. 응답 자체는 확인했으며 성공으로 집계하지 않았다.
- UI KR 자문: 실제 `claim-v3`, stop 종료, 625토큰, 실행 약 80초, cold-start 대기 약 295초. E1/E2/E3의 사건번호·법원·날짜·페이지·원문 해시 연결 통과. 수동 검토에서는 조건부 결론과 한계는 확인했지만 유효성 판단 및 반론을 법률적으로 인증하지 않았다.
- 서버리스 로그는 실제 adapter SHA, base SHA, vLLM 0.28.0, `claim-v3` LoRA 경로와 응답 alias를 입증한다. 하지만 세 기능 배포 검증 전체는 실패했다.
- 첫 워커 컨테이너는 18:53:12 UTC에 RunPod 측 stop/remove 후 재시작되었다. 확보한 로그로 정확한 원인은 확정하지 못했다. 후속 두 응답은 수신했다.
- 새 private 템플릿 `6rorkup97n`은 삭제했다. 기존 템플릿 `99icte8riw`로 복구했고 현재 min=0, **max=0**, idle=5초, FlashBoot OFF이다. 요청에 따라 켜지는 max=1 운영은 새 모델 검증 실패 때문에 남기지 않았다.
- 실제 학습 Pod 0, 네트워크 볼륨 0, 엔드포인트 GPU Pod 0, 진행/대기 요청 0, Auto-pay OFF, 시간당 현재 과금 0을 확인했다. health/worker 요약에 과거 running 메타데이터가 남는 경우가 있어 최종 종료 판정에는 control-plane Pod 부재와 시간당 과금을 사용했다.
- 두 개의 실제 0 관측 간격: 92.3초. 종료 관측 시각: 2026-09-09T04:04:46.728500+09:00.
- $15 충전 직후 잔액 $19.839799 → 종료 시 잔액 **$9.177295**. 충전 이후 관측된 차감 **$10.662504**로 추가 실행 상한 $12 안에 있다.
- 이번 작업 첫 관측 잔액 $11.655852에 수동 충전 $15를 더한 기준의 전체 관측 차감은 **$17.478556**이다. 위 충전 이후 비용과 범위가 다르다. 초기 실패·복구·평가·초기 v2 smoke 비용도 포함한다.
- 최종 balance 차감과 당시 billing API 집계는 일치하지 않았다. billing 집계에는 일부 최신 사용분이 아직 반영되지 않은 것으로 보이며, 확정 청구서 금액이라고 주장하지 않는다. 원 응답은 closing_audit.json에 보존했다.
- 마지막 Secure H200은 28.76분, GPU $4.59/시간 구성이었고, 서버리스 실제 관측 단가는 $2.72/시간이었다. GPU 준비·다운로드·재시작 시간도 비용에 포함된다.
- HF 임시 전송 dataset 4개는 전송 검증 직후 삭제했다. 유료 HF Jobs와 DeepInfra는 사용하지 않았다. 로컬 증거·복구 checkpoint 및 비공개 최종 모델은 보존했다.

| 학습/평가 Pod | 용도 | 최종 상태 |
|---|---|---|
| polzuxjjvfma91 | 초기 judge JSON 검증 실패 | 삭제 확인 |
| 16lzkoxcswettn | 초기 judge 기준 미달 | 삭제 확인 |
| hff4ulh1b7ye73 | 거절된 1-step RL 및 복구 검증 | 삭제 확인 |
| q3b735wnfkyf5h | 후속 RL, 감시 코드 오인으로 조기 종료 | 삭제 확인, step8 복구 |
| 65hkcrf8xyd5ow | step16 학습/평가, judge TAB 파싱 중단 | 삭제 확인, step16 복구 |
| s8pcn4cgdmolee | 잔여 평가와 최종 export/full reload | 검증 후 삭제 확인 |

추가 예산을 요청하지 않고 유료 반복을 중단했다. 다음 작업은 재개 checkpoint를 활용하되, 도면 생성 반복과 제공 판례 적용 채점의 결함을 먼저 해결하고 세 기능 UI 검증을 다시 통과시키는 것이다. 지금 상태를 성공한 서버리스 v3 배포로 인수인계하지 않는다.

임시 Windows 절전 방지는 해제 완료했고, 학습/서버리스 종료 감시 프로세스도 남아 있지 않다. 상시 로컬 테스트 화면은 열려 있으나 두 생성 API는 유료 요청 전 중지를 확인했다.
